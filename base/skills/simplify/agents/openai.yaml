interface:
  display_name: "Simplify"
  short_description: "Behavior-preserving anti-slop cleanup pass"
  default_prompt: "Run $simplify on the current changes to remove needless complexity, dead code, and AI-shaped overengineering without changing behavior."
policy:
  allow_implicit_invocation: true
