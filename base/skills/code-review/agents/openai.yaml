interface:
  display_name: "Code Review"
  short_description: "High-signal diff review that learns the repo"
  default_prompt: "Use $code-review to review the current diff for blockers, should-fixes, and nits before merge."
policy:
  allow_implicit_invocation: true
